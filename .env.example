CITYCHATBOT_ENV=local
APP_BASE_URL=http://127.0.0.1:3100
NEXT_PUBLIC_APP_ENV=local
NEXT_PUBLIC_APP_URL=http://127.0.0.1:3100

# Configure only when the AI task is active. Never commit a real value.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=

# Server-only security material. Load from a vault/secret manager; never commit values.
CSRF_SECRET=
TENANT_CREDENTIAL_KEY=
TENANT_CREDENTIAL_KEY_VERSION=
